generator client {
    provider = "prisma-client-py"
    interface = "asyncio"
}

datasource db {
    provider = "mongodb"
    url      = "mongodb+srv://mantissa6789:Mantis%402510@cluster0.9ramotn.mongodb.net/iss"
}

// wdh

// type Dimensions {
//     width Int
//     depth Int
//     height Int
// }

model Item {
    id String @id @default(uuid()) @map("_id")
    itemId String @unique @map("itemId")
    name String
    width Int
    depth Int
    height Int
    priority Int
    expiryDate String
    usageLimit Int
    preferredZone String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}

model Container {
    id String @id @default(uuid()) @map("_id")
    containerId String @unique @map("containerId")
    zone String
    width Int
    depth Int
    height Int
    itemsIds String[]
}

model Zone{
    id String @id @default(uuid()) @map("_id")
    name String
    containersIds String[]
}